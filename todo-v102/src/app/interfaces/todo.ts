export interface Todo {
    id: number;
    title: string;
    description: string;
  } // much like a struct in c++
  // previously made in edit-todo.page.ts
  // cut and pasted here so multiple files can access